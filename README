docker inspect db 
(ca sa iau adresa ip a containerului db pentru conectarea cu pagent)

in container db:
INTRA IN CONTAINER DE DB:
docker exec -ti db psql -U postgres -d postgres

sa vezi ce tabele ai:
\dt

descriere table_name:
\d table_name


STERGE CONTAINERE:
docker container rm $(docker ps -aq)

STERGE IMAGINI:
docker image rm $(docker images -a -q)


TODOs:
PUT/api/countries/:id;
-> la PUT la cities test care are lang long integer; desi la countries nu e,
ai putea sa adaugi testul de verificare de tip si aici; si la post la fel